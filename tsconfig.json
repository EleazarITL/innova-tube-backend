{
    "compilerOptions": {
      "target": "es6",                           // Versión de ECMAScript a la que se compilará el código
      "module": "commonjs",                      // Módulos CommonJS para Node.js
      "strict": true,                            // Habilita verificaciones estrictas de tipo
      "esModuleInterop": true,                   // Permite la interoperabilidad con ES6 módulos
      "skipLibCheck": true,                      // Omitir verificación de tipos en los archivos .d.ts
      "forceConsistentCasingInFileNames": true,  // Forzar consistencia en el uso de mayúsculas en nombres de archivo
      "outDir": "./build",                       // Carpeta de salida para los archivos compilados
      "rootDir": "./src"                         // Carpeta raíz de tus archivos TypeScript
    },
    "include": ["src/**/*.ts"],                  // Incluir todos los archivos TypeScript en la carpeta `src`
    "exclude": ["node_modules"]                  // Excluir `node_modules` de la compilación
}  